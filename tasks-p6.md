# P6: Harness 使用层与工业化落地

> 来源：结合 DeerFlow harness 使用方式、OPC 当前代码与文档现状、工业落地关注点形成的改进清单。
> 格式遵循项目任务系统：`- [ ] 任务描述 <!-- files: ... --> <!-- context: ... -->`。
> 执行标注：`review`=需要人工取舍/评审后再做；`order`=必须在前置任务完成后顺序执行；`auto`=可由 Agent 小步直接执行；`decision`=已确认的决策。
> 测试策略：每条任务优先跑定向测试，避免一次性全量测试影响本机性能。

## 1. 使用入口与诊断体验

- [x] 增加 opc init / doctor / config validate 使用入口 <!-- files: src/opc/cli.py, src/opc/config.py, README.md, tests/ --> <!-- context: 责任角色=PM/Ops；输入=README 快速开始、opc.example.toml、当前 CLI 命令；输出=init/doctor/config validate 命令与使用文档；依赖关系=无；完成标准=新用户可通过 doctor 看到 API key、opc.toml、workspace、索引目录、可用命令状态，config validate 能发现无效 role/profile --> <!-- review: 工业落地入口，需确认命令命名和检查项范围 -->
- [x] 修复配置与文档漂移 <!-- files: opc.example.toml, README.md, docs/DOCS_STRUCTURE.md, tests/ --> <!-- context: 责任角色=QA；输入=opc.example.toml 中 roles=["engineer"]、README UI/CLI 描述、DOCS_STRUCTURE.md 中 QUICK_UPLOAD.md；输出=一致的配置示例与文档索引；依赖关系=无；完成标准=config validate/单元测试覆盖示例配置，文档索引不再引用不存在文件，README 对 UI 与 CLI 入口表述与代码一致 --> <!-- auto: 小范围文档与示例修复 -->

## 2. Run / Trace 使用体验

- [x] 增加 runs / trace 一等 CLI UX <!-- files: src/opc/cli.py, src/opc/run_store.py, src/opc/ui.py, tests/ --> <!-- context: 责任角色=Engineer/QA；输入=run_events.jsonl、run_trace.json、run_metrics.json、现有 UI 历史记录；输出=opc runs list、opc trace show、opc trace summary 等命令；依赖关系=无；完成标准=可按项目列出 run_id、最终状态、耗时、失败原因，并能查看单次 run 的事件和指标摘要 --> <!-- auto: 先做只读命令，不修改运行时 -->
- [x] 增加 trace schema 版本与兼容性测试 <!-- files: src/opc/run_store.py, src/opc/workflow.py, docs/plan/architecture.md, tests/ --> <!-- context: 责任角色=Engineer/QA；输入=run_events.jsonl、run_trace.json、run_metrics.json；输出=trace_schema_version 字段、schema 文档、兼容性测试；依赖关系=无；完成标准=旧 trace 可读取，新 trace 带版本号，测试覆盖缺字段/新增字段/失败 run 三种情况 --> <!-- auto: 小步增强 run trace 稳定性 -->

## 3. Workflow Pack 与复用范式

- [x] 设计 OPC workflow pack 使用规范 <!-- files: docs/plan/workflow.md, docs/claude/standards.md, .claude/skills/, tasks-p6.md --> <!-- context: 责任角色=Architect/PM；输入=DeerFlow skills 使用范式、现有 .claude/skills、本项目角色/任务/验收标准；输出=workflow pack 的 manifest、输入、输出、权限、验收、示例结构；依赖关系=依赖配置与文档漂移修复；完成标准=至少定义 bugfix/review/docs-update 三类 workflow pack 规范，并明确哪些是 Claude 协作 skill、哪些是 OPC runtime workflow --> <!-- review: 需要产品边界取舍 -->
- [x] 增加 golden run walkthrough 与验收样例 <!-- files: examples/, docs/runs/, workspace/demo-*/, README.md --> <!-- context: 责任角色=Growth/QA；输入=现有 quickstart_minimal.py、sample_tasks.md、demo workspace artifacts、run trace；输出=安装→运行→查看 trace→验收的 2-3 个可复现样例；依赖关系=依赖 runs/trace CLI UX；完成标准=每个样例包含命令、输入任务、预期产物、trace 查看方式、验收标准和失败排查说明 --> <!-- order: 依赖 trace CLI 完成 -->

## 4. 权限治理与插件生态

- [x] 增加权限 profile 与危险操作审批策略 <!-- files: src/opc/config.py, src/opc/tools/, src/opc/security/, docs/plan/architecture.md, tests/ --> <!-- context: 责任角色=Ops/Architect；输入=ToolDefinition.permission、command whitelist、dangerous command audit、SecurityConfig；输出=read-only/write/execute/dangerous profile 与 per-role tool grants；依赖关系=无；完成标准=默认 profile 不允许危险命令直接执行，危险命令可配置为 deny/approval/audit，测试覆盖各 profile 的工具可见性和拒绝路径 --> <!-- review: 工业安全边界，需确认默认策略 -->
- [x] 增加插件与 MCP 接入安全契约 <!-- files: src/opc/tools/tool_registry.py, docs/plan/architecture.md, examples/opc_plugins/, tests/ --> <!-- context: 责任角色=Architect/Engineer；输入=load_plugin_tools、ToolDefinition 字段、DeerFlow MCP/tool 配置模式；输出=插件 manifest、权限声明、禁用默认策略、最小 sample plugin、MCP roadmap；依赖关系=依赖权限 profile 任务；完成标准=未声明权限的插件不加载，样例插件可被发现并显示审计信息，文档说明 MCP 何时接入及边界 --> <!-- order: 依赖权限 profile 完成 -->

## 5. Harness 分层对齐与上下文治理

- [x] 建立 OPC Harness L1-L6 能力矩阵 <!-- files: docs/plan/architecture.md, README.md, tasks-p6.md --> <!-- context: 责任角色=Architect/PM；输入=当前 L1 信息边界、L2 工具系统、L3 执行编排、L4 记忆与状态、L5 评估与观测、L6 约束校验与恢复分层，以及 DeerFlow 的 runtime/middleware/memory/sandbox/skills 使用方式；输出=OPC 当前能力、已有证据、短板、P6 对应任务的矩阵；依赖关系=无；完成标准=矩阵能说明 OPC 哪些层已具备、哪些只需产品化、哪些暂不做，避免盲目复制 DeerFlow 的 super agent 宽度 --> <!-- auto: 文档归纳，不引入新功能 -->
- [x] 补充信息边界与上下文治理契约 <!-- files: docs/plan/architecture.md, docs/knowledge-retrieval-design.md, src/opc/workflow.py, tests/ --> <!-- context: 责任角色=Architect/QA；输入=现有 ROLE_CONTEXT_SECTIONS、RAG 检索、角色 schema、memory 边界规则，以及 DeerFlow context engineering / summarization / memory post-run 思路；输出=每类角色可见上下文、禁止注入内容、RAG 来源标注、memory 使用前验证规则；依赖关系=无；完成标准=文档明确 L1 信息边界，测试或轻量检查覆盖角色上下文 allowlist、RAG source attribution、memory 不覆盖当前文件事实 --> <!-- review: 需要确认上下文边界粒度 -->
- [x] 定义 thread / session / run 的使用边界 <!-- files: docs/plan/workflow.md, src/opc/cli.py, src/opc/run_store.py, README.md --> <!-- context: 责任角色=PM/Engineer；输入=现有 run_id、resume、workspace artifacts、RunStore，以及 DeerFlow thread/run/checkpoint 使用体验；输出=thread、session、run、artifact、checkpoint 的 OPC 语义定义和用户可见命令规划；依赖关系=依赖 runs / trace CLI UX；完成标准=用户能理解何时新建 run、何时 resume、哪些状态可恢复、哪些中间产物仅用于回放，不把一次 run 与长期 memory 混淆 --> <!-- order: 依赖 trace CLI 完成 -->

## 6. P6 后工业化缺口评估

- [x] 形成单人本地使用边界评估 <!-- files: docs/plan/roadmap.md, docs/plan/architecture.md, tasks-p6.md --> <!-- context: 责任角色=CEO/PM/Ops；输入=P6 完成后的 CLI、trace、权限 profile、workflow pack、插件契约；输出=单人本地使用场景下的项目切换、权限默认值、审计归属、本地 memory、run 冲突处理和数据存储边界；依赖关系=依赖 P6 阶段验收；完成标准=明确 OPC v1 继续保持单人本地优先，不进入团队/多用户模式，并列出单人场景下仍需补齐的 P7 候选任务 --> <!-- decision: 当前阶段专注单人使用，不规划团队/多用户落地 -->
- [x] 建立工业化评测基准与回归样例集 <!-- files: tests/, examples/, docs/runs/, docs/plan/success.md --> <!-- context: 责任角色=QA/Growth；输入=P6 golden run、run trace、workflow pack、角色输出 schema；输出=覆盖 bugfix/review/docs-update/config-drift/failed-tool/rework-resume 的基准任务集和成功率指标；依赖关系=依赖 golden run walkthrough；完成标准=每类任务有固定输入、预期产物、验收标准、trace 证据和失败判定，用于后续版本比较任务成功率、人工介入率、成本和耗时 --> <!-- order: 依赖 golden run 完成 -->
- [x] 评估部署运维与私有化运行边界 <!-- files: Dockerfile, docs/plan/roadmap.md, docs/plan/architecture.md, README.md --> <!-- context: 责任角色=Ops/Architect；输入=现有 Docker 镜像任务、CLI 本地运行方式、workspace/artifacts/index 存储；输出=本地单机、持久服务、私有部署三种模式的资源、数据、日志、备份、升级和回滚边界；依赖关系=依赖 P6 权限与 trace 任务；完成标准=明确哪些能力适合本地 CLI，哪些需要 server/control plane，哪些暂不进入近期 Roadmap --> <!-- review: P6 后置评估，不直接实现服务化 -->
- [x] 补充合规与数据边界清单 <!-- files: docs/plan/architecture.md, docs/knowledge-retrieval-design.md, README.md --> <!-- context: 责任角色=Ops/QA；输入=memory、RAG、trace、tool audit、plugin、MCP、安全配置；输出=敏感数据、密钥、客户代码、日志、外部工具出域、memory 删除与审计保留边界清单；依赖关系=依赖信息边界与插件安全契约；完成标准=用户能判断哪些数据会被读取、存储、发送、长期保留或删除，且与 P6 的权限 profile 和 memory 边界一致 --> <!-- order: 依赖信息边界与插件安全契约完成 -->

## 7. Runtime 协议、Guardrail、Replay 与 Memory 治理

- [x] 定义统一阶段执行契约 <!-- files: src/opc/workflow_spec.py, src/opc/workflow.py, src/opc/roles.py, docs/plan/workflow.md, tests/ --> <!-- context: 责任角色=Architect/Engineer/QA；输入=现有角色 prompt、WorkflowState、ToolDefinition、stage artifact、QA schema；输出=StageContract/StageResult/StageValidation/TransitionPolicy 的完整 DAG 契约与文档说明；依赖关系=无；完成标准=每个核心阶段都能声明 input schema、role、tools、output schema、artifact、validation、next transition、conditional branch、failure branch、retry policy、parallel_group 和 sub_workflow，测试覆盖缺字段、schema 不匹配、非法 transition、失败分支四类情况 --> <!-- decision: 已确认按完整 DAG 规划，允许先分步落地 -->
- [x] 将角色输出与阶段契约绑定 <!-- files: src/opc/roles.py, src/opc/workflow.py, src/opc/workflow_spec.py, tests/ --> <!-- context: 责任角色=Engineer/QA；输入=PM/Engineer/QA/Ops/Growth 当前输出格式、QA rollback_stage、artifact_paths、完整 DAG 阶段契约；输出=角色输出校验、阶段产物校验、失败原因结构化记录；依赖关系=依赖统一阶段执行契约；完成标准=角色输出不符合契约时 workflow 不进入下一阶段，并在 run_events.jsonl 中记录 validation_failed 事件、失败分支候选和可读诊断 --> <!-- order: 依赖阶段契约完成 -->
- [x] 建立阶段契约 golden case 回归集 <!-- files: tests/, examples/, docs/runs/ --> <!-- context: 责任角色=QA；输入=bugfix/review/docs-update/config-drift/failed-tool/rework-resume 等典型任务、现有 run trace、角色 schema；输出=最小 golden case 数据集与定向 pytest；依赖关系=依赖角色输出与阶段契约绑定；完成标准=每类任务至少有一个固定输入、预期阶段输出、预期 transition、预期失败分支和失败判定，定向测试可验证契约没有回退 --> <!-- order: 依赖契约绑定完成 -->
- [x] 设计 Guardrail Policy 统一策略层 <!-- files: src/opc/security/, src/opc/config.py, src/opc/tools/tool_registry.py, docs/plan/architecture.md, tests/ --> <!-- context: 责任角色=Architect/Ops；输入=SecurityConfig、ToolDefinition.permission、side_effect、timeout、command whitelist、max_tool_rounds、cost hard limit；输出=GuardrailPolicy 最小模型，统一表达 allow/deny/approval/audit/stop 策略；依赖关系=无；完成标准=读操作默认允许，写入和常见开发命令按角色授权，明显危险命令默认 deny，发布或外部影响动作必须 approval，文档说明默认策略和可配置策略，测试覆盖 allow/deny/audit/approval 四类路径 --> <!-- decision: 已确认默认采用开发友好策略 -->
- [x] 将 Guardrail 接入 Agent / Tool / Workflow 执行路径 <!-- files: src/opc/agent.py, src/opc/workflow.py, src/opc/tools/, src/opc/security/, tests/ --> <!-- context: 责任角色=Engineer/QA；输入=Agent tool loop、tool registry、workflow transition、run_store、开发友好 Guardrail 默认策略；输出=执行前策略检查、执行后审计事件、被拦截动作的结构化错误；依赖关系=依赖 Guardrail Policy 统一策略层；完成标准=危险工具调用、越权写入、外部影响动作、超预算、超轮次均产生一致 guardrail_blocked、guardrail_warning 或 approval_required 事件，并可在 trace 中定位原因 --> <!-- order: 依赖 Guardrail Policy 完成 -->
- [x] 增加人工审批点与熔断决策记录 <!-- files: src/opc/workflow.py, src/opc/run_store.py, docs/plan/workflow.md, tests/ --> <!-- context: 责任角色=Ops/QA；输入=human_intervention、rollback_stage、retry_policy、cost_warning、tool failure、approval_required 事件；输出=approval_required、circuit_breaker_open、rollback_decision 等事件和 artifact 记录；依赖关系=依赖 Guardrail 接入执行路径；完成标准=需要人工接管时 trace 能说明触发规则、待审批动作、可选决策和默认停止行为，测试覆盖审批缺失时不继续执行 --> <!-- order: 依赖 Guardrail 接入完成 -->
- [x] 定义 Trace Inspect 只读能力 <!-- files: src/opc/run_store.py, src/opc/cli.py, docs/plan/workflow.md, README.md, tests/ --> <!-- context: 责任角色=PM/Engineer/QA；输入=run_events.jsonl、run_trace.json、run_metrics.json、artifact_paths、stage_logs；输出=opc inspect 或 opc trace inspect 的命令设计、只读 inspect 数据模型和文档；依赖关系=依赖 trace schema 版本任务；完成标准=用户能按 run_id 查看阶段时间线、输入摘要、输出 artifact、工具调用、失败点、指标和最终状态，不重新调用模型或工具 --> <!-- decision: 已确认第一版只做只读 Inspect，不做重跑 -->
- [ ] 实现最小可用 Trace Inspect CLI <!-- files: src/opc/cli.py, src/opc/run_store.py, src/opc/ui.py, tests/ --> <!-- context: 责任角色=Engineer/QA；输入=Trace Inspect 设计、RunStore.load、现有 runs/trace CLI UX；输出=只读 inspect 命令、阶段时间线输出、失败诊断摘要；依赖关系=依赖 Trace Inspect 只读能力定义；完成标准=可从已有 artifacts 重建执行时间线，缺失事件时给出兼容提示，定向测试覆盖完整 run、失败 run、旧 trace 三种输入 --> <!-- order: 依赖 inspect 设计完成 -->
- [ ] 建立 Memory 分层与生命周期规范 <!-- files: docs/plan/architecture.md, docs/plan/workflow.md, src/opc/memory.py, tests/ --> <!-- context: 责任角色=Architect/PM/QA；输入=当前 memory.py、thread/session/run/artifact/checkpoint 边界、RAG 设计、用户偏好与项目决策记忆需求；输出=user/project/workflow/run/artifact memory 分层定义、scope、created_at、updated_at、expires_at、source、confidence 字段规范；依赖关系=无；完成标准=文档能明确 Memory 与 RAG、Session、Checkpoint、Artifact 的差异，短期 run 状态不会写入长期 memory，测试或检查覆盖 scope 与过期字段 --> <!-- decision: 已确认关键长期写入需要确认 -->
- [ ] 增加 Memory 检索注入与冲突处理策略 <!-- files: src/opc/memory.py, src/opc/workflow.py, docs/plan/architecture.md, tests/ --> <!-- context: 责任角色=Engineer/QA；输入=Memory 分层规范、角色上下文 allowlist、RAG source attribution、当前文件事实优先原则；输出=按任务/角色/scope 检索 memory 的策略、superseded 冲突标记、当前文件验证规则；依赖关系=依赖 Memory 分层与生命周期规范；完成标准=Memory 不全量注入 prompt，冲突时当前文件事实优先，trace 记录本次使用的 memory id/source/reason，测试覆盖过期、冲突、越权角色不可见三类情况 --> <!-- order: 依赖 Memory 分层规范完成 -->
- [ ] 增加 Memory 写入审核与安全边界 <!-- files: src/opc/memory.py, src/opc/security/, docs/plan/architecture.md, README.md, tests/ --> <!-- context: 责任角色=Ops/QA；输入=用户偏好、项目决策、安全策略、敏感信息、trace、RAG 索引；输出=memory write policy、敏感内容拒写规则、人工确认点、删除/更新路径；依赖关系=依赖 Memory 检索注入与冲突处理策略；完成标准=用户偏好、项目决策、安全策略、外部引用等关键长期 memory 写入前有可审计确认路径，凭证和临时调试内容默认不入长期 memory，并能删除或 supersede 旧 memory --> <!-- order: 依赖 Memory 检索策略完成 -->

## 8. 阶段验收

- [ ] P6 项验收：harness 使用层达到工业落地最小闭环 <!-- files: tasks-p6.md, src/opc/cli.py, src/opc/config.py, src/opc/run_store.py, docs/, examples/ --> <!-- context: 责任角色=QA/Ops；输入=P6 所有任务产物；输出=验收记录；依赖关系=依赖 P6 前述任务完成；完成标准=(1) 新用户可 init/doctor/config validate；(2) run trace 可通过 CLI 查看；(3) 配置示例和文档索引无漂移；(4) workflow pack 规范可指导新增任务类型；(5) 权限 profile 与插件安全契约有测试或文档证据；(6) L1-L6 能力矩阵说明 OPC 已有能力、短板和不做项；(7) thread/session/run 边界清晰，memory 不与一次性 run 状态混淆；(8) 完整 DAG 阶段契约、开发友好 Guardrail 默认策略、只读 Trace Inspect、Memory 分层与关键写入审核均有文档或测试证据；(9) P6 后工业化缺口已形成单人本地优先的 P7 候选方向，不把团队治理、多用户、服务化能力误标为近期目标 --> <!-- order: 依赖前述所有 P6 任务完成 -->
